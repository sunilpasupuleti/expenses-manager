on:
  push:
    branches:
      - master
name: 🚀 Deploy to Ec2
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      # - name: 🚚 Get latest code
      #   uses: actions/checkout@v3
      # - name: 📂 Deploy in EC2 Instance
      #   uses: easingthemes/ssh-deploy/@main
      #   env:
      #     SSH_PRIVATE_KEY: ${{secrets.AWS_PEM_KEY}}
      #     SOURCE: "./"
      #     REMOTE_HOST: ${{secrets.HOST_NAME}}
      #     REMOTE_USER: ${{secrets.USER_NAME}}
      #     TARGET: "/home/ubuntu/expenses-manager/"
      - name: Executing commands in AWS
        uses: appleboy/ssh-action@master
        with:
          host: ${{secrets.HOST_NAME}}
          username: ${{secrets.USER_NAME}}
          key: ${{secrets.AWS_PEM_KEY}}
          port: 22
          script: |
            sudo ln -s "/home/ubuntu/.nvm/versions/node/v18.16.0/bin/node" "/usr/local/bin/node"
            sudo ln -s "/home/ubuntu/.nvm/versions/node/v18.16.0/bin/npm" "/usr/local/bin/npm"
            npm install pm2 -g
            sudo ln -s "/home/ubuntu/.nvm/versions/node/v18.16.0/bin/pm2" "/usr/local/bin/pm2"
            cd /home/ubuntu/expenses-manager/backend/
            touch config/.env
            echo -e '''PORT=3000
            FIREBASE_DATABASE_URL=https://expensesmanager-da065-default-rtdb.firebaseio.com/
            FIREBASE_STORAGE_BUCKET=expensesmanager-da065.appspot.com
            LOGPATH=logs
            MONGO_DATABASE_DEV=development
            MONGO_DATABASE_PRODUCTION=production
            MONGO_DATABASE_URL=mongodb+srv://sunil:Sunil%40123@cluster0.lrqxoge.mongodb.net/<database>?retryWrites=true&w=majority
            ''' >> .env
            pm2 start "npm start" --name="expenses-manager-backend"
